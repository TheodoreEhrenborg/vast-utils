
Requirements:

vast ai API key in `~/.config/vastai/vast_api_key`

TODO try aws keys

This is roughly imitating the interface of terraform/AWS batch. See https://github.com/Beneficial-AI-Foundation/vericoding/pull/311 for an example of how to use those tools.
